# Инструкция: Как разместить репозиторий на GitHub

Пошаговое руководство по созданию репозитория marathon-2.0 на GitHub.

---

## Шаг 1: Подготовка (если Git не установлен)

### macOS
```bash
# Проверить, установлен ли Git
git --version

# Если нет, установить через Homebrew
brew install git
```

### Windows
Скачать и установить: https://git-scm.com/download/win

### Linux
```bash
sudo apt-get install git  # Ubuntu/Debian
sudo yum install git       # CentOS/RHEL
```

---

## Шаг 2: Настройка Git (первый раз)

```bash
# Указать имя и email
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"
```

---

## Шаг 3: Создание репозитория на GitHub

1. Зайти на https://github.com
2. Нажать **"New repository"** (зелёная кнопка)
3. Заполнить:
   - **Repository name:** `marathon-2.0`
   - **Description:** `Программа саморазвития из 4 встреч`
   - **Public** (если планируете открыть всем позже)
   - ❌ НЕ ставить галочки "Add README" (у нас уже есть)
4. Нажать **"Create repository"**

---

## Шаг 4: Инициализация локального репозитория

```bash
# Перейти в директорию с файлами
cd /path/to/marathon-2.0

# Инициализировать Git
git init

# Добавить все файлы
git add .

# Сделать первый коммит
git commit -m "Initial commit: Marathon 2.0 structure and documentation"
```

---

## Шаг 5: Связать с GitHub и загрузить

```bash
# Добавить удалённый репозиторий
# Замените YOUR-USERNAME на ваш GitHub username
git remote add origin https://github.com/YOUR-USERNAME/marathon-2.0.git

# Указать основную ветку
git branch -M main

# Загрузить на GitHub
git push -u origin main
```

---

## Шаг 6: Проверка

Откройте в браузере:
```
https://github.com/YOUR-USERNAME/marathon-2.0
```

Должны увидеть:
- ✅ README.md как главную страницу
- ✅ Всю структуру файлов и папок
- ✅ Лицензию и другие файлы

---

## Шаг 7: Настройка репозитория (опционально)

### 7.1 Добавить описание
- Зайти в Settings → General
- В поле "Description" добавить: "Программа саморазвития из 4 встреч для выхода из застревания"
- Добавить topics: `personal-development`, `productivity`, `self-improvement`, `russian`

### 7.2 Включить Issues
- Settings → General → Features
- ✅ Issues (для вопросов и предложений)

### 7.3 Включить Discussions (опционально)
- Settings → General → Features
- ✅ Discussions (для обсуждений)

### 7.4 Настроить GitHub Pages (опционально)
Если хотите веб-версию документации:
- Settings → Pages
- Source: Deploy from a branch
- Branch: main, folder: /(root)
- Save

Документация будет доступна по адресу:
```
https://YOUR-USERNAME.github.io/marathon-2.0/
```

---

## Дальнейшая работа с репозиторием

### Внесение изменений

```bash
# Изменить файлы
# ...

# Добавить изменения
git add .

# Сделать коммит с описанием
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

### Создание новой ветки для экспериментов

```bash
# Создать и переключиться на новую ветку
git checkout -b feature/new-feature

# Внести изменения, коммит
git add .
git commit -m "Add new feature"

# Загрузить ветку на GitHub
git push -u origin feature/new-feature

# Создать Pull Request на GitHub
```

### Синхронизация изменений

```bash
# Получить изменения с GitHub
git pull origin main
```

---

## Типичные команды Git

```bash
# Статус (какие файлы изменены)
git status

# История коммитов
git log --oneline

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# Посмотреть изменения
git diff

# Создать новую ветку
git branch feature-name

# Переключиться на ветку
git checkout branch-name

# Удалить ветку
git branch -d branch-name
```

---

## Структура коммитов (рекомендации)

Используйте понятные сообщения:

**Хорошие примеры:**
```
git commit -m "Add questionnaire template"
git commit -m "Update meeting 2 structure"
git commit -m "Fix typos in concepts.md"
git commit -m "Add examples for tracker usage"
```

**Плохие примеры:**
```
git commit -m "Update"
git commit -m "Fix"
git commit -m "Changes"
```

---

## Что делать, если что-то пошло не так

### Забыли добавить файл в последний коммит
```bash
git add forgotten-file.md
git commit --amend --no-edit
git push --force
```

### Нужно откатить изменения
```bash
# Откатить изменения в файле (до коммита)
git checkout -- filename.md

# Откатить последний коммит (удалить)
git reset --hard HEAD~1
```

### Конфликт при push
```bash
# Получить изменения
git pull origin main

# Разрешить конфликты в файлах
# (открыть файлы, исправить конфликты)

# Добавить разрешённые файлы
git add .

# Завершить merge
git commit -m "Resolve merge conflicts"

# Загрузить
git push
```

---

## Приватный репозиторий (пока работаете)

Если хотите сначала поработать приватно:

1. При создании репозитория выберите **Private**
2. Работайте как обычно
3. Когда готовы открыть:
   - Settings → Danger Zone
   - Change repository visibility
   - Выберите **Public**

---

## Добавление соавторов

Если работаете в команде:

1. Settings → Collaborators
2. Add people
3. Ввести GitHub username коллеги
4. Отправить приглашение

---

## GitHub Actions (автоматизация, опционально)

Можно настроить автоматические проверки:

Создать файл `.github/workflows/validate.yml`:

```yaml
name: Validate Markdown

on: [push, pull_request]

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Check broken links
        uses: gaurav-nelson/github-action-markdown-link-check@v1
```

Это будет автоматически проверять ссылки в документации.

---

## Полезные ресурсы

- **GitHub Docs:** https://docs.github.com
- **Git Cheatsheet:** https://education.github.com/git-cheat-sheet-education.pdf
- **Markdown Guide:** https://www.markdownguide.org/

---

## Чек-лист: Всё ли готово?

- [ ] Git установлен и настроен
- [ ] Репозиторий создан на GitHub
- [ ] Файлы загружены (git push)
- [ ] README.md отображается корректно
- [ ] Все ссылки работают
- [ ] Добавлено описание репозитория
- [ ] Topics добавлены
- [ ] Issues включены
- [ ] Лицензия видна на странице репозитория

---

## Что дальше?

После размещения на GitHub:

1. **Поделиться ссылкой** с командой
2. **Создать первый Issue** с планами развития
3. **Добавить примеры** в материалы/участникам/примеры/
4. **Собрать обратную связь** от первых участников
5. **Обновлять документацию** по мере появления опыта

---

Успехов с репозиторием! 🚀

Если возникнут вопросы, создайте Issue в репозитории или обратитесь к сообществу GitHub.
