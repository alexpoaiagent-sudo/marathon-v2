# 🚀 Быстрая настройка Google Drive (5 минут)

> **Этот способ НАМНОГО проще, чем OAuth2 Playground!**

---

## Что вы уже сделали ✅

- ✅ Создали проект в Google Cloud Console
- ✅ Включили Google Drive API
- ✅ Настроили OAuth consent screen
- ✅ Создали Desktop app credentials
- ✅ Скачали credentials.json

---

## Что осталось (всего 2 шага!)

### Шаг 1: Установить Python библиотеки

```bash
cd /home/user/VK-offee

# Установка необходимых библиотек
pip install google-auth-oauthlib google-auth-httplib2 google-api-python-client
```

**Время:** ~30 секунд

---

### Шаг 2: Запустить скрипт

Убедитесь, что файл `credentials.json` находится в папке `/home/user/VK-offee/`

```bash
# Перейдите в папку
cd /home/user/VK-offee

# Запустите скрипт
python3 get-gdrive-token.py
```

**Что произойдёт:**
1. ✅ Откроется браузер с Google авторизацией
2. ✅ Войдите под своим аккаунтом
3. ✅ Нажмите **"Разрешить"** (Allow)
4. ✅ Скрипт автоматически:
   - Получит refresh token
   - Сохранит в `token.json`
   - Создаст `.env` файл с credentials
   - Покажет все данные для `.mcp.json`

**Время:** ~1 минута (включая авторизацию)

---

## После запуска скрипта

Скрипт создаст 2 файла:

### 1. `.env` (переменные окружения)
```bash
GOOGLE_CLIENT_ID=ваш-client-id
GOOGLE_CLIENT_SECRET=ваш-secret
GOOGLE_REFRESH_TOKEN=ваш-refresh-token
GOOGLE_FOLDER_ID=120x7kqYeV0Vb4TLbdCC0esv0WkF5JROC
```

### 2. `token.json` (полные данные токена)
```json
{
  "refresh_token": "...",
  "token": "...",
  "client_id": "...",
  "client_secret": "..."
}
```

---

## Шаг 3: Настроить .mcp.json

Скопируйте пример:
```bash
cp .mcp.json.example .mcp.json
```

Отредактируйте `.mcp.json` - замените значения на реальные из `.env`:

```json
{
  "mcpServers": {
    "quint-code": {
      "command": "/usr/local/bin/quint-code",
      "args": ["serve"],
      "cwd": "/home/user/VK-offee",
      "env": {
        "QUINT_PROJECT_ROOT": "/home/user/VK-offee"
      }
    },
    "gdrive": {
      "command": "mcp-server-gdrive",
      "args": [],
      "env": {
        "GOOGLE_CLIENT_ID": "ваш-client-id-из-env",
        "GOOGLE_CLIENT_SECRET": "ваш-secret-из-env",
        "GOOGLE_REFRESH_TOKEN": "ваш-refresh-token-из-env",
        "GOOGLE_FOLDER_ID": "120x7kqYeV0Vb4TLbdCC0esv0WkF5JROC"
      }
    }
  }
}
```

---

## Проверка подключения

После настройки `.mcp.json` перезапустите Claude Code и проверьте:

```bash
# Я смогу выполнять команды типа:
# - Прочитать список файлов из Google Drive
# - Скачать документы
# - Конвертировать Google Docs → Markdown
```

**Попросите меня:**
```
"Claude, прочитай список всех файлов из Google Drive папки VK-Coffee"
```

Если всё работает - я покажу список файлов! 🎉

---

## Автоматический импорт

После успешного подключения, просто скажите:

```
"Claude, импортируй все документы из Google Drive в B.COFFEE,
распредели их по структуре SRT 3×3"
```

**Я автоматически:**
1. ✅ Получу список всех файлов (173 документа)
2. ✅ Конвертирую Google Docs → Markdown
3. ✅ Распределю по семействам F0-F9
4. ✅ Добавлю YAML frontmatter
5. ✅ Создам README в каждой папке
6. ✅ Закоммичу изменения

---

## Troubleshooting

### Ошибка: "No module named 'google_auth_oauthlib'"
**Решение:** Установите библиотеки
```bash
pip install google-auth-oauthlib google-auth-httplib2 google-api-python-client
```

### Ошибка: "credentials.json not found"
**Решение:** Убедитесь, что файл находится в `/home/user/VK-offee/`
```bash
ls -la /home/user/VK-offee/credentials.json
```

### Ошибка: "Access blocked: Authorization Error"
**Решение:** Добавьте свой email в test users
1. Google Cloud Console → OAuth consent screen
2. Test users → Add users
3. Добавьте свой email

### Браузер не открывается
**Решение:** Скопируйте URL из терминала и откройте вручную

---

## Безопасность 🔒

**НЕ коммитьте в Git:**
- ❌ `credentials.json`
- ❌ `token.json`
- ❌ `.env`
- ❌ `.mcp.json`

Все эти файлы уже в `.gitignore` ✅

---

## Следующие шаги

1. ✅ Запустите `python3 get-gdrive-token.py`
2. ✅ Настройте `.mcp.json` с реальными данными
3. ✅ Перезапустите Claude Code
4. ✅ Попросите меня импортировать документы

**Готово!** 🚀

---

**Версия:** 1.0
**Дата:** 2026-01-23
**Автор:** Claude Code

**Время выполнения:** ~5 минут
**Сложность:** ⭐ Очень просто
